# congestion_factor: (type: float) 0 < range <= 1 where 0 is no requests and 1 is a request at every intersection
congestion_factor = 0.2

# grid_size: (type: tuple of two ints) max x and y values for a grid starting at (0,0) where x > 0 and y > 0
grid_size = (10, 10)